template: true
valuesFilePath: ./values.yml

pipelines:
  - name: testcron
    steps:
      - name: basic{{$i}}
        type: Bash
        execution:
          onExecute:
            - echo hi
      - name: basic1
        type: Bash
        configuration:
          inputSteps:
            - name: basic0
        execution:
          onExecute:
            - echo hi
