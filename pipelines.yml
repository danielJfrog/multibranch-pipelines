template: true
valuesFilePath: ./values.yml


pipelines:
  - name: testcron
    steps:
      - name: basicly
        type: Bash
        execution:
          onExecute:
            - echo hi
      {{ range $i, $e := until 6001 }}
      - name: basic{{$i}}
        type: Bash
        configuration:
          inputSteps:
            - name: basicly
        execution:
          onExecute:
            - echo hi
      {{ end }}
