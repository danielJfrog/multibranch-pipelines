template: true
valuesFilePath: ./values.yml

pipelines:
  - name: testcron
    steps:
      - name: basic
        type: Matrix
        stepMode: Bash
        configuration:
          multiNode: true
          stepletMultipliers:
            environmentVariables:
              {{ range $i, $e := until 100 }}
              - foo: "bar{{$i}}"
              {{ end }}
        configuration:
          runtime:
            type: host
        execution:
          onExecute:
            - echo hi
